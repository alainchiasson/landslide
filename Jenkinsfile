pipeline {
 /* decalrative definition here */
    agent none 
    stages {
        stage {
            agent { 
                dockerfile true
                label 'docker'
            }
        }
    }
}
